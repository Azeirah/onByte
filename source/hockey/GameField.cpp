#include "GameField.h"

namespace hockey {
	GameField::GameField() : Field(1.0f) {
	    // haha, nothing!
	    // everything is done in the subclass, because it isn't generic like a cube, rectangle or sphere U_u
	}
}

